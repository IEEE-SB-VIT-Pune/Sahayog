import 'package:flutter/material.dart';

class CalendarPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        body: (Center(child: Text('Calendar', style: TextStyle(fontSize: 60)))),
      );
}
