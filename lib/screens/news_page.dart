import 'package:flutter/material.dart';

class NewsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        body: (Center(child: Text('News', style: TextStyle(fontSize: 60)))),
      );
}
